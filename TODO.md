# TODO

- import missing data from uptime backup to uptime instance
- fix unhealthy beszel and beszel agent
- remove redundant contianer monitoring from uptime kuma (and podman socket if unused after that)
- add more to monitor in beszel agent (disks?)
- add missing services to uptime kuma (beszel, zerobyte)
- finish configuring zerobyte (mounts, backups, notifications)
- finish configuring beszel (security, notifications)
- read service logs after reboot to ensure no warnings/errors appear
- manually import failed radarr/sonarr downloads
- run beszel-agent on hv01 directly as well as a container?
- reconfigure bichon
- add shows sass and mia wanted
- upgrade immich db password
- setup tsidp again and configure services to use it starting with incusos
