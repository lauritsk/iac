Keep `.gitignore` allowlist-style; explicitly allow every tracked file and preserve its logical grouping.
Check `justfile` first; prefer its recipes, using custom commands only when needed.
Never add comments or content that was not explicitly requested.
Never create an unsigned commit. If commit signing fails, stop and report the failure.
